<div id="<%= dom_id group %>">
  <p>
    <strong>Name:</strong>
    <%= group.name %>
  </p>

  <% if group.users.include?(current_user) %>
    <%= button_to 'Leave', leave_group_path(group), method: :delete %>
  <% else %>
    <%= button_to 'Join', join_group_path(group), method: :post %>
  <% end %>

  <h3>Members:</h3>
  <ul>
    <% group.users.each do |user| %>
      <li><%= user.email %></li>
    <% end %>
  </ul>

</div>
